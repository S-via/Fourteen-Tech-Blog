<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Homepage</title>
</head>
<body>
    <h1>All Blog Posts</h1>
    <ul>
        {{#each blogs}}
        <li>
            <h2><a href="/blog/{{this.id}}">{{this.title}}</a></h2>
            <p>Posted by: {{this.username}}</p>
            <p>{{this.content}}</p>
        </li>
        {{/each}}
    </ul>
</body>
</html>








{{!-- <!--display all posts  -->
{{each blogs as|blog|}}

<div class="card">
  <div class="card-header">
    <a href ="/blog/{{blog.id}}">{{blog.title}}</a>
  </div>
  <div class="card-body">
    <h5 class="card-title">Posted by:{{blog.username}}</h5>
    <p class="card-text">{{blog.content}}.</p>
  </div>
</div>

{{/each}}
 --}}

<!-- when clicked on title of blog post it will direct them to -->
<!-- Add comment with button to submit-->
